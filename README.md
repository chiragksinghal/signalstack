# Project Name

A Dockerized full-stack app with a backend API and database, designed to demonstrate production-style local development with containers.

## Tech Stack
- Docker + Docker Compose
- Backend: (fill in)
- Database: (fill in)
- Frontend: (fill in)

## Quickstart

1) Install Docker Desktop  
2) From the project root, run:
```bash
docker compose up --build
